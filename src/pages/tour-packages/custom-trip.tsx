import { FC } from 'react';
import ContainerCustomTrip from '@/components/Containers/CustomTrip/CustomTrip';

const customTrip: FC = () => {
  return <ContainerCustomTrip />;
};

export default customTrip;
